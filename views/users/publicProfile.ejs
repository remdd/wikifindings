<% include ../partials/header %>

<h4>User profile of <%= user.username %></h4>

<% if(user.publicBio) { %>
	<p class="shownComment shownCommentText"><%= user.publicBio %></p>

<% } %>

<hr />

<% if(findings.total > 0) { %>

	<h4><%= user.username %> has posted <%= findings.total %> Finding<% if(findings.total > 1) { %>s<% } %> :</h4>

	<% include ../partials/paginationDiv %>

	<% include ../partials/findingsindex %>

	<% include ../partials/paginationDiv %>


<% } %>

<% include ../partials/footer %>